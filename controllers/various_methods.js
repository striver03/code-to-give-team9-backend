//nprmal questions (after base questions)
const optionVala = async (req,res,currentCollection) => {
    try {
        const val = req.body;   // It would be an array containing the options marked
        //students -> student -> documents(question) -> ? , ans 
        //location -> location -> drug
        //age -> [18+] -> count,female/male
            //   [25 se 30]
        
        
        /*
         data = {
            question : "This is a question",
            options : ["option1", "option2", "option3", "option4"...]
            markedAns: Int value (4)
         }
        */
       // add data database -> question -> ans
       // open ai (api) -> (base questions)
    } catch (error) {
        
    }
};

// //base questions -> 5 quetsions
// //last question ke baad
// const afterLastBaseQuestion = async (req, res) => {
// };
